## Null Pass

The API-only auth service shared across the Null apps. It exposes routes for user auth (register, login, sessions, profile), service entitlements, health checks, manages sessions, payments and much more.